<Window x:Class="AniDBmini.ImportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Background="{DynamicResource WindowBackgroundBrush}"
        Title="Import Mylist" Height="105" Width="275" AllowDrop="True" ResizeMode="NoResize" WindowStartupLocation="CenterOwner" ShowInTaskbar="False" Closing="OnClose" Drop="OnDrop">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35" />
            <RowDefinition Height="35" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBox x:Name="importFilePath" Grid.ColumnSpan="3" Grid.Row="0" Margin="5" Height="24" Drop="OnDrop" IsReadOnly="True" PreviewDragEnter="OnDragOver" PreviewDragOver="OnDragOver" />
        <Button Content="Browse" Grid.Row="1" Height="28" Margin="5,0,5,0" VerticalAlignment="Center" Click="BrowseOnClick" />
        <Button x:Name="importStart" Content="Start" Style="{StaticResource NuclearGreenButton}" Grid.Column="2" Grid.Row="1" Height="28" Margin="5,0,5,0" VerticalAlignment="Center" IsEnabled="False" Click="StartOnClick" />
    </Grid>
</Window>